# PRD: Backlog & Prioritization Sandbox

## Goals
- Visual dashboard (status, top RICE, epic impact)
- Roadmap timeline with dependencies
- API endpoints for ranked backlog & metrics

## API
- GET /backlog
- GET /ranked
- GET /metrics/summary
- GET /roadmap
- POST /export/github (placeholder)
